TODO: rsync backups

CHANGES
-----

throttling for karma

bot:
	common: move out of modules, split up
	use requests, not urllib
	filter out messages tagged with @batch
	add a better way to restart/reload/stop bot remotely (UNIX sockets?)
		update systemd unit and add to repo.
	do not crash if exception was thrown in a handle_raw

documentation on the bot's internals.
add config option controlling whether to display module name in bot messages


NEW FUNCTIONALITY
-----------------

calendar
sedbot functionality
koran lookup
distrowatch lookup
HN/lobsters lookup
oxford dictionary lookup
wikipedia lookup
GUS searching
Google searching
DDG searching
Gemini/Gopher support for urls
tell <user> command
lastseen command
talklike command

evaluation:
	python evaluation
	wolfram|alpha evaluation
	sed evaluation
	dc/bc evaluation
	common lisp evaluation
